# PostHog public project token (phc_...) of the "Kalp portfolio" project. Leave empty locally: analytics stay off.
VITE_PUBLIC_POSTHOG_KEY=
# Always /ingest (the vercel.json rewrite proxies to PostHog on the site's own origin).
VITE_PUBLIC_POSTHOG_HOST=/ingest
